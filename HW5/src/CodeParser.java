public class CodeParser {
    private String comp, dest = "null", jump = "null", addr;
// parses a line of code into the correct sections, ie comp, dest, and jump for c-instructions or addr for A-instructions
    public boolean parse(String s) {
        return true;
    }

    private void parseJump(String s) {
        //parses the jump instr into jump and comp values
    }

}
